export enum HouseholdRole {
  OWNER = 'OWNER',
  MEMBER = 'MEMBER',
}

export enum PetType {
  DOG = 'DOG',
  CAT = 'CAT',
  OTHER = 'OTHER',
}

export enum TaskType {
  FEED = 'FEED',
  WALK = 'WALK',
  CLEAN = 'CLEAN',
  OTHER = 'OTHER',
}

export enum TaskScheduleType {
  DAILY = 'DAILY',
  ONCE = 'ONCE',
}
